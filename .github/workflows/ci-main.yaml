name: CI - Main Branch
on:
  workflow_dispatch:
  push:
    branches:
      - main
jobs:
  common:
    uses: raghudevopsb83/github-actions-reusable/.github/workflows/ci-main.yaml@main
    with:
      component_name: user



